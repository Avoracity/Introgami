const express = require("express");
const router = express.Router();

// API routes 
router.get('/introgami_db', (req, res) => {
  // code to handle the request and send a response
});

module.exports = router;
